// use cinnamos_abi::proc::ProcessId;

// use crate::mem::alloc::slab::SlabAllocator;

// #[derive(Debug)]
// pub struct Process {
//     id: ProcessId,
// }

// static PROC_ALLOC: SlabAllocator<4, Process> = SlabAllocator::new();
